#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 * Print some text with function prinft
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
