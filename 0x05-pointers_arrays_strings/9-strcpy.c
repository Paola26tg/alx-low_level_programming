#include "main.h"
/**
 * _strcpy - copies the string pointed to by src
 * @dest: pointed to
 * @src: string pointed
 * Return: dest
 */
char *_strcpy(char *dest, char *src)
{
	char ptr;

	ptr = *src;
	dest = ptr;
	return(dest);
}
